import { packagesCirculatingSupply } from './packages-circulating-supply'

describe('packagesCirculatingSupply', () => {
  it('should work', () => {
    expect(packagesCirculatingSupply()).toEqual('packages-circulating-supply')
  })
})
